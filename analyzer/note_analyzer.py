from typing import List
from LifeSyncBackend.schemas.note_schema import NoteData, NoteDataRequest

def note_analyzer(data: List[NoteData]):
    recommendation = [
    ]

    insight = [
    ]

    return [recommendation, insight]
